# ตัวแปร Environment สำหรับ Frontend

# การตั้งค่า API
# สำหรับ Cross-LAN (Frontend และ Backend อยู่คนละเครือข่าย):
# เอาเครื่องหมาย # ออก และใส่ URL ของ Backend ที่เข้าถึงได้จาก Internet
VITE_API_BASE=https://your-backend-domain.com/api

# สำหรับ Same-LAN (Frontend และ Backend อยู่เครือข่ายเดียวกัน):
# ใส่เครื่องหมาย # ไว้ข้างหน้า หรือใช้ค่า '/api' (relative path)
# VITE_API_BASE=/api
