# Environment variables for Express backend
PORT=3001
PICO_IP=192.168.1.100

# Mock mode - set to 'true' to simulate Pico W without hardware
# Useful for frontend development and testing
MOCK_MODE=true

# MQTT Configuration
# Use public broker or your own (Mosquitto, HiveMQ, etc.)
MQTT_ENABLED=true
MQTT_BROKER=mqtt://broker.hivemq.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=home-iot-backend

# Scheduling / Database
# Storage type: 'file' or 'mongodb' (default: file)# Environment variables for Express backend
PORT=3001
PICO_IP=192.168.1.100

# Mock mode - set to 'true' to simulate Pico W without hardware
# Useful for frontend development and testing
MOCK_MODE=false

# MQTT Configuration
# Use public broker or your own (Mosquitto, HiveMQ, etc.)
MQTT_ENABLED=true
MQTT_BROKER=mqtt://broker.hivemq.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=home-iot-backend

# Scheduling / Database
# Storage type: 'file' or 'mongodb' (default: file)
STORAGE_TYPE=file
# MongoDB Atlas connection string (required if STORAGE_TYPE=mongodb)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/home-iot
# Default timezone for schedules
TIMEZONE=Asia/Bangkok
# Third-party time API (default: worldtimeapi.org)
TIME_API_URL=https://worldtimeapi.org/api
# Google Maps Timezone API key (optional, for better reliability)
# Get key from: https://developers.google.com/maps/documentation/timezone/get-api-key
GOOGLE_TIMEZONE_API_KEY=
# Scheduler interval in ms (optional)
SCHEDULER_INTERVAL_MS=30000

STORAGE_TYPE=file
# MongoDB Atlas connection string (required if STORAGE_TYPE=mongodb)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/home-iot
# Default timezone for schedules
TIMEZONE=Asia/Bangkok
# Third-party time API - Options:
# - https://worldtimeapi.org/api (default, synced with pool.ntp.org)
# - http://worldclockapi.com/api
TIME_API_URL=https://worldtimeapi.org/api
# Scheduler interval in ms (optional)
SCHEDULER_INTERVAL_MS=10000
