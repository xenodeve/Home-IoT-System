# ตัวแปร Environment สำหรับ Express backend
PORT=3001
# สำหรับ Pico W Simulator: ใช้ localhost:5000
# สำหรับ Pico W จริง: ใช้ 192.168.1.100
PICO_IP=192.168.1.100

# โหมดจำลอง - ตั้งเป็น 'true' เพื่อจำลอง Pico W โดยไม่ต้องใช้ฮาร์ดแวร์จริง
# มีประโยชน์สำหรับการพัฒนาและทดสอบ Frontend
# หมายเหตุ: เมื่อใช้ Pico W Simulator (pico_simulator.py) ตั้ง MOCK_MODE=false
MOCK_MODE=false

# การตั้งค่า MQTT
# ใช้ public broker หรือของตัวเอง (Mosquitto, HiveMQ, etc.)
MQTT_ENABLED=true
MQTT_BROKER=mqtt://broker.hivemq.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=home-iot-backend

# การตั้งเวลา / ฐานข้อมูล
# ประเภทการจัดเก็บ: 'file' หรือ 'mongodb' (default: file)
STORAGE_TYPE=file
# สตริงการเชื่อมต่อ MongoDB Atlas (จำเป็นถ้า STORAGE_TYPE=mongodb)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/home-iot
# โซนเวลาปริยายสำหรับกำหนดการ
TIMEZONE=Asia/Bangkok

# Third-party time API - ตัวเลือก:
# - https://worldtimeapi.org/api (default, sync กับ pool.ntp.org)
# - http://worldclockapi.com/api
TIME_API_URL=https://worldtimeapi.org/api

# Google Maps Timezone API key (optional, เพื่อความน่าเชื่อถือที่ดีขึ้น)
# รับ key จาก: https://developers.google.com/maps/documentation/timezone/get-api-key
GOOGLE_TIMEZONE_API_KEY=

# ช่วงเวลาการทำงานของ Scheduler ใน ms (optional)
SCHEDULER_INTERVAL_MS=10000
